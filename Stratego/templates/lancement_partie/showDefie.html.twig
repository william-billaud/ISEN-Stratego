{% extends 'base.html.twig' %}

{% block body %}

    <h4>Defie : </h4>
    {% for defie in parties %}
        <div class="row">
            <div class="col s12">
                <div class='card'>
                    <div class="card-content">
                        <div class="left">
                            <p>{{ defie.joueur1.username|e }}</p>
                        </div>
                        <div class="right">
                            <a href="{{ path('accepteDefie',{'idPartie':defie.id}) }}" class="btn waves-effect waves-light" id="accepter" >Accepter
                                <i class="material-icons right">directions_walk</i>
                            </a>

                            <a href="{{ path('refuseDefie',{'idPartie':defie.id}) }}" class="btn waves-effect wawes-dark" id="refuser" >Refuser
                                <i class="material-icons right">directions_walk</i>
                            </a>
                        </div>

                    </div>
                    <div class="card-action">

                    </div>
                </div>
            </div>
        </div>
    {% endfor %}
    </div>

{% endblock %}

{% block title %}

{% endblock %}

